{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "filekind needs native file pickers and nothing else. All filesystem access happens in Rust, inside explicit commands — the webview is never handed a path it can read on its own.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message"
  ]
}
